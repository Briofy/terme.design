//global state

// sidebar state (open/close)
export const sidebar = ref<boolean>(true);

// search state
export const search = ref<string | null>("");
export const isSearching = ref<boolean>(false);
